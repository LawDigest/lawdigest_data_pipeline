# ===================================================================
# Airflow DAGs - Essential Python Dependencies
# ===================================================================
# This file is curated to contain only the libraries necessary for
# running the Airflow DAGs in the 'dags' directory.
#
# To install:
# pip install -r airflow/requirements.txt
# ===================================================================

# Airflow provider for MySQL connection
# This is the recommended way to enable MySQL connections in Airflow.
apache-airflow-providers-mysql
pymysql
IPython
langchain_community

# Core library for date/time manipulation used in DAGs
pendulum==2.1.2

# For loading environment variables from .env files
python-dotenv==1.1.1

# For making HTTP requests in various data fetching jobs
requests==2.32.3

# For parsing HTML content in web scraping jobs
beautifulsoup4==4.13.4
lxml==6.0.0

# For data manipulation and analysis
pandas==2.3.1

# For interacting with OpenAI and vector DB
openai==1.99.6
langchain==0.3.27
qdrant-client==1.9.2

# For displaying progress bars (used in some tools called by DAGs)
tqdm==4.66.4

# Gemini AI Integration
langchain-google-genai
google-generativeai